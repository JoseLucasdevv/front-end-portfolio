import { ScrollYDirective } from './scroll-y.directive';

describe('ScrollYDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollYDirective();
    expect(directive).toBeTruthy();
  });
});
