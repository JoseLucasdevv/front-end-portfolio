import { HeaderBorderDirective } from './header-border.directive';

describe('HeaderBorderDirective', () => {
  it('should create an instance', () => {
    const directive = new HeaderBorderDirective();
    expect(directive).toBeTruthy();
  });
});
